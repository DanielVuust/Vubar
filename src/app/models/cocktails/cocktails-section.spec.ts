import { CocktailsSection } from './cocktails-section';

describe('CocktailsSection', () => {
  it('should create an instance', () => {
    expect(new CocktailsSection("test", "test", [])).toBeTruthy();
  });
});
